worker_threads: 15

hash_type: sha512

state_output: mixed

file_roots:
  base:
      - /srv/salt/states

pillar_roots:
  base:
      - /srv/salt/pillar

fileserver_backend:
  - git

gitfs_remotes:
  - https://github.com/saltstack-formulas/apache-formula.git
